{
  "name": "advanced-dictation",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "generate:editor-data": "node scripts/generate-editor-data.mjs",
    "predev": "npm run generate:editor-data",
    "dev": "vite",
    "prebuild": "npm run generate:editor-data",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.2",
    "@types/react-dom": "^19.2.2",
    "@vitejs/plugin-react": "^5.1.0",
    "typescript": "^5.9.3",
    "vite": "^7.1.11"
  }
}
